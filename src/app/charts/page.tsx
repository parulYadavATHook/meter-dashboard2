import Chart from "../../../components/chart";

export default function ChartPage() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center bg-gradient-to-r from-blue-100 to-blue-200 p-4 w-full">
      <Chart />
    </div>
  );
}
